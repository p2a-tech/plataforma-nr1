import { PageSkeleton } from "@/components/ui/loading";

// /conformidade/acoes: cards de resumo + quadro de planos por status.
export default function Loading() {
  return <PageSkeleton titulo="Plano de ação · acompanhamento" />;
}
