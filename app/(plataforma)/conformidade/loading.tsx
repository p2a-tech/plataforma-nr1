import { PageSkeleton } from "@/components/ui/loading";

// /conformidade: checklist + cartões eSocial + trilha de auditoria.
export default function Loading() {
  return <PageSkeleton titulo="Conformidade & eSocial" />;
}
