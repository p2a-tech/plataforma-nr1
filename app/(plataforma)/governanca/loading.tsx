import { PageSkeleton } from "@/components/ui/loading";

// /governanca: toggles de privacidade e ética — cards + chave/valor.
export default function Loading() {
  return <PageSkeleton titulo="Governança & LGPD" />;
}
