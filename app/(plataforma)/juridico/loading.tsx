import { PageSkeleton } from "@/components/ui/loading";

// /juridico: base legal, DPIA, evidências — várias seções de texto + cards.
export default function Loading() {
  return <PageSkeleton titulo="Compliance Jurídico" />;
}
