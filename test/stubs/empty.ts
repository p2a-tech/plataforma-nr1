// Stub para o pacote `server-only` durante os testes em ambiente Node.
// `server-only` lança em qualquer ambiente que não seja um Server Component;
// aqui ele é neutralizado para permitir importar módulos como lib/pgr.ts.
export {};
